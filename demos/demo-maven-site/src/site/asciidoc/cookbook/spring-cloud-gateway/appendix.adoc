[[appendix]]
= 附录A: 通用应用程序属性

可以在 `application.properties` 文件内，`application.yml` 文件内或作为命令行开关指定各种属性。
本附录提供了常见的Spring Cloud Gateway属性列表以及对使用它们的底层类的引用。

NOTE: 属性贡献可能来自类路径上的其他jar文件，因此您不应将其视为详尽的列表。另外，您可以定义自己的属性。

include::_configprops.adoc[]
